---
# vars file for findfetch_role
# Makes sure the folder fetchz exists and desired files are created for testing

src_file_loc: "/home/{{ ansible_user_id }}/fetchz" #Delcaring source file path
dest_file_loc: "/home/{{ ansible_user_id }}/fetchz" #Declaing destination file path

# Makes sure the folder fetchz exists and desired files are created for testing

